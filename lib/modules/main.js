import generateReport from './generateReport';
import ensureFolderPath from './ensureFolderPath';
import scrapePage from './scrapePage';
import scrapePageBySourceId from './scrapePageBySourceId';
import scrapeSourceById from './scrapeSourceById';

export default {
  generateReport,
  ensureFolderPath,
  scrapePage,
  scrapePageBySourceId,
  scrapeSourceById
};
